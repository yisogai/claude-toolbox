# コストレポート — ${task_name}

- 日付: ${date_jst}（JST） / 時間帯: ${start_jst} 〜 ${end_jst}（経過 ${duration}）
- 実処理時間: ${active_text}
- 対象範囲: ${scope}
- タスク内容: ${task_desc}

## モデル別内訳

| モデル | 入力(uncached) | キャッシュ書込(5m/1h) | キャッシュ読取 | 出力 | 料金(USD) |
| --- | ---: | ---: | ---: | ---: | ---: |
${model_rows}

## 合計

- **Fable（従量課金・要報告）: $$${payg_usd} / ¥${payg_jpy}**
- その他（Max20x込み・参考）: $$${included_usd} / ¥${included_jpy}
- 総合計: $$${total_usd} / ¥${total_jpy}（参考レート 1 USD = ${usd_jpy} 円）
${unknown_note}${budget_note}
--- 単価 as_of: ${pricing_as_of}${stale_note} / 生成: ${generated_at_jst}
