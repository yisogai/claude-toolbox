# AGENTS.md — Codex への作業規約（claude-toolbox/codex-bridge が配布するテンプレート）

<!-- このファイルは Claude Code（統括）が Codex に実装を委譲するときの共通規約。
     リポジトリ固有の規約はこの下に追記する。Codex は AGENTS.md を「最も近い1枚」だけ読む（上書き型・32KiB上限）ため、
     階層は1段（リポジトリルートのみ）に留める。 -->

## 言語
- 最終メッセージ・コミットメッセージ・コード内コメントは日本語。コード・識別子・ファイル名・コマンドはそのまま。

## 作業の範囲（最重要）
- 依頼文に書かれた **範囲だけ** を変更する。ついで修正・リファクタリング・フォーマット変更はしない（気づいた点は最終メッセージの「提案（未対応）」に書く）。
- 以下は **明示的に禁止**（「禁止と書かれていないから許可」と解釈しない）:
  - `git commit` / `git push` / `git stash` / `git checkout` / `git reset` などの履歴・作業ツリー操作
  - 依頼に無いファイルの作成・削除・リネーム、依存パッケージの追加・更新
  - 既存テストの削除・スキップ化・期待値の書き換えによる「通過」
  - 設定ファイル（`~/.*`、`.env*`、CI 設定）の変更、ネットワーク越しの送信（push・外部 API 呼び出し）
  - 受入条件に無い挙動の追加

## 実装の進め方
- 着手前に、依頼の「目的 / 範囲 / 非目標 / 受入条件」を自分の言葉で 3〜6 行に書き直し、解釈の余地がある点は「〜と解釈した」と明示して進める（質問して止まらない）。
- コメント・ドキュメント・テスト・命名は実装と食い違うことがある。食い違いを見つけたら、**コードの実挙動**（実行・テスト）で正を確認してから直す。
- バグの根本原因を特定したら、同じ誤りのクラスが同じ関数・モジュールの他の分岐・他の入力条件に潜んでいないかを修正前に点検する。
- 変更のたびにテストか型チェックを回せるなら回す。まとめて最後に、ではなく小刻みに。

## 完了の条件
- 受入条件を **実際に検証してから**（テスト実行・実行結果の確認）完了と言う。検証していないことを「動くはず」「修正しました」と書かない。
- 「堅牢に」「任意の入力で」のような要件では、想定外入力（空・巨大・不正エンコーディング・境界）を最低 1 つ実際に当てる。
- テスト・lint に失敗が残る場合、その事実を最終メッセージの **冒頭** に書く。成功したことにしない。
- 未検証の仮定に基づいて進んだ部分は「[未検証] …」として最終メッセージに列挙する。

## 最終メッセージの形式（統括側が機械的に読む。この見出しを必ずこの順で）
```
## 結果: 完了 | 一部完了 | 失敗
## 変更ファイル
- <path>: <何を変えたか 1 行>
## 実行した検証
- <コマンド>: <結果（pass/fail と要点）>
## 受入条件の充足
- [x|ー] <受入条件 1>: <根拠>
## 未検証・仮定
- ...
## 提案（未対応）
- ...
```

<!-- ここから下はリポジトリ固有の規約 -->

## リポジトリ固有の規約（claude-toolbox）
- Python は 3.11+。外部依存を追加しない（標準ライブラリのみ）。
- codex-bridge のスクリプトは `scripts/codex_lib.py` の共通関数（パス解決・atomic 書込・JSONL 追記）を再利用する。
- コメントは既存の密度・日本語スタイルに合わせる（設計判断・罠には由来の注記を残す）。
- テストは `python3 -m pytest codex-bridge/tests/ -q` で全通過させる。テストは `tests/mock_codex.py` を使う既存方式に合わせ、実機 codex を呼ばない。
- 書込は依頼された範囲のファイルと `codex-bridge/tests/` への追記のみ。
